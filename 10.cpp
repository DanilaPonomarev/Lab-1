#include <stdio.h>
#include <conio.h>
int main()
{ 
    float y,x,q;
    y = 0.5;
    x = 2.0;
    q = x+y;
    if (q > x)
        {
        printf("ДА");
        }
    else
        {
        printf("НЕТ");
        }
}
