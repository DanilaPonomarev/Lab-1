#include <stdio.h>
#include<math.h>
int main()
{
   int k = 0, x, i = 2;
    scanf("%d", &x);
    while (i >= 0) {
        k += (x % 10) * powf(10, i--);
        x /= 10;
    }
    printf("%d", k);
}
